---
import Breadcrumbs from '@/components/Breadcrumbs.astro'
import Link from '@/components/Link.astro'
import PageHead from '@/components/PageHead.astro'
import ProjectCard from '@/components/ProjectCard.astro'
import Layout from '@/layouts/Layout.astro'
import { getAllProjects } from '@/lib/data-utils'

const projects = await getAllProjects()
---

<Layout>
  <PageHead slot="head" title="About" />
  <img class="aspect-3/2 object-cover ..." src="https://i.imghippo.com/files/CgO8693yk.png"/>
  <Breadcrumbs items={[{ label: 'About', icon: 'lucide:info' }]} />

  <section>
    <div class="min-w-full">
      <div class="prose mb-8">
        <p class="mb-4">
          The "Brucy" meme is prevalent on platforms like X and Telegram, where community create 
          value and visibility that utilize the iconic meme on
           <Link
            href="https://x.com/brucyonsol"
            class="text-foreground"
            external
            underline>X</Link
          >, and <Link
            href="https://t.me/brucyonsol"
            class="text-foreground"
            external
            underline>Telegram</Link
          >. It's used to express determination, celebrate small victories, 
          and humorously depict the struggle against overwhelming odds.
        </p>
        <p>
          To learn more about the philosophy behind brucy, check out the
          following blog post: <Link
            href="/blog/the-state-of-static-blogs"
            class="text-foreground"
            underline>Greater Value of the Community Project</Link
          >.
        </p>
      </div>

      <h2 class="mb-4 text-2xl font-medium">Example Projects Listing</h2>
      <div class="flex flex-col gap-4">
        {projects.map((project) => <ProjectCard project={project} />)}
      </div>
    </div>
  </section>
</Layout>
